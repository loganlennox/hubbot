
exports.routeInput = (msg) => {
  console.log(`Routing... "${msg}"`);

  // 1 - Check if message is explicit

  // 2 - Check if message is command

  // 3 - Check if message is trigger

  console.log(`"${msg}" is a normal message.`);
};
